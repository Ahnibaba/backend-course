{
  "name": "chapter_3",
  "version": "1.0.0",
  "description": "A full stack todo application that uses a Nodejs backend, a PostgreSQL database, a Prisma ORM and jwt authentication",
  "main": "server.js",
  "scripts": {
    "dev": "prisma generate --no-engine && node --watch --env-file=.env ./src/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "@prisma/client": "^6.12.0",
    "@prisma/extension-accelerate": "^2.0.2",
    "bcryptjs": "^3.0.2",
    "dotenv": "^17.2.0",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.16.3"
  },
  "devDependencies": {
    "prisma": "^6.12.0"
  }
}
